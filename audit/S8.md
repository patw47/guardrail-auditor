# S8 Verification — Final Polish (remediation + README + OpenAPI examples + demo seed)

**Verifier:** isolated, read-only (Read/Grep/Bash). Only write: this report.
**Date:** 2026-06-22 · **Repo:** /home/acer/guardrail-auditor · **HEAD:** 5453da6

## L1 gate (real output)

| Tool | Result | Exit |
|------|--------|------|
| `./.venv/bin/ruff check .` | `All checks passed!` | 0 |
| `./.venv/bin/mypy .` | `Success: no issues found in 46 source files` | 0 |
| `./.venv/bin/pytest -q` | `85 passed, 1 warning in 1.82s` | 0 |

(The single warning is a 3rd-party Starlette/httpx deprecation, not our code.)

## Criteria

| # | Criterion | Verdict | Evidence |
|---|-----------|---------|----------|
| 1 | L1 ruff/mypy/pytest all green | PASS | exits 0/0/0; 85 passed (see above) |
| 2 | Every RULES rule_id has a Remediation; `remediate()` pure + fenced (unknown→None); test asserts it | PASS | `core/remediation.py:11-57` REMEDIATIONS has all 4 rule_ids; `remediate()` is `REMEDIATIONS.get(rule_id)` (returns None on miss). `tests/test_remediation.py:10-14` asserts every rule has a non-None rem with summary+snippet; `:34-35` asserts `remediate("NOT_A_RULE") is None` |
| 3 | REMEDIATIONS keyset == CONTROL_MAP keyset; each summary references the cited control | PASS | RULES/CONTROL_MAP/REMEDIATIONS all = {OPEN_SSH, S3_PUBLIC_BUCKET, UNENCRYPTED_STORAGE, PUBLIC_DB}. `tests/test_remediation.py:17-19` asserts `set(REMEDIATIONS)==set(CONTROL_MAP)`. Summaries (`core/remediation.py`): OPEN_SSH→"§5.2", S3_PUBLIC_BUCKET→"§2.1.4", UNENCRYPTED_STORAGE→"§2.2.1", PUBLIC_DB→"A.8.20"; matches CONTROL_MAP primary controls. `tests/test_remediation.py:22-31` asserts each token in summary |
| 4 | FindingOut.remediation computed in `_finding_out` from rule_id (NOT persisted); live probe carries summary/snippet | PASS | `api/routes.py` `_finding_out` calls `remediate(fr.rule_id)` (comment: "derived from rule_id (no schema change)") and builds `RemediationOut`. `api/schemas.py` adds `RemediationOut` + `FindingOut.remediation: RemediationOut\|None=None`. LIVE: POST /api/scans → 201, 4 findings, each carries remediation.summary+snippet (S3 also cli) |
| 5 | README guarded-real: test asserts real commands/endpoints + referenced files exist | PASS | `tests/test_readme.py` asserts tokens `pip install -e ".[dev]"`, `uvicorn main:app`, `pytest`, `tools/demo_seed.py`; endpoints `/api/scans`, `/api/scans/repo`, `/api/scans/{id}/rescan`, `/openapi.json`; and `multi_violation.tf` + `docs/coverage_matrix.md` `.exists()`. 3/3 tests PASS. Both files exist on disk (339B / 2.4K) |
| 6 | /openapi.json RepoScanRequest carries terragoat example | PASS | LIVE: RepoScanRequest schema `examples: [{"repo_url": "https://github.com/bridgecrewio/terragoat"}]`. Sourced from `api/schemas.py` `RepoScanRequest.model_config.json_schema_extra` |
| 7 | demo_seed OFFLINE + IDEMPOTENT; test asserts exactly 1 demo scan after 2 runs; *.db gitignored | PASS | `tools/demo_seed.py` uses `UploadedFilesSource` on bundled `multi_violation.tf` (no clone/network). `seed()` deletes prior DEMO_TAG rows then saves → idempotent. `tests/test_demo_seed.py:21-25` asserts `len(demos)==1` after `seed();seed()`. LIVE: 2 runs → exactly 1 demo scan. `git check-ignore guardrail.db demo.db` → both returned; `git ls-files \| grep '\.db$'` → empty |

## Constraints

| ID | Constraint | Verdict | Evidence |
|----|-----------|---------|----------|
| C1 | No schema change: `core/tables.py` unchanged vs HEAD | PASS | `git diff --stat HEAD core/tables.py` empty. Remediation computed, not a column |
| C2 | No new runtime dependency: pyproject deps unchanged | PASS | `git diff HEAD pyproject.toml` empty |
| C3 | Pipeline/detectors/scoring/compliance/parsing unchanged | PASS | `git diff --stat HEAD rules/ core/scoring.py core/compliance.py core/parsing/ core/pipeline.py` empty. Full diff touches only README.md, api/routes.py, api/schemas.py, prompts.md, tests/test_dashboard.py, web/static/* (tracked) + new core/remediation.py, models/remediation.py, tools/demo_seed.py, tools/__init__.py, tests/test_{remediation,readme,demo_seed}.py, docs/screenshots/gate-rejection.png — all in the allowed set |
| C4 | Remediation deterministic + NO LLM | PASS | `core/remediation.py` imports only `models.remediation`; `models/remediation.py` imports only `dataclasses`. No requests/http/urllib/socket/subprocess/openai/anthropic/llm/client. Pure data + `dict.get` |

## Tampering hunt

- No `skip`/`xfail`/`skipif`/`pytest.mark`/`assert True` in any S8 test (grep empty).
- README test asserts load-bearing literal tokens (`pip install -e ".[dev]"`, `uvicorn main:app`, real endpoints) AND `Path.exists()` on referenced fixtures — not trivially true.
- Remediation tests assert keyset EQUALITY (`set(REMEDIATIONS)==set(CONTROL_MAP)`) and per-rule control-reference tokens, plus the unknown-rule fence — not merely "not empty".
- Demo-seed test asserts exact count `==1` after a double run (true idempotency, not just "populated").

## VERDICT: PASS

All 7 criteria PASS; constraints C1–C4 PASS; no tampering. Remediation is per-rule, control-consistent, and computed at the API boundary (no schema change, no new dependency, no LLM). README is guarded-real against a lying-README test. Demo seed is offline + idempotent + db-gitignored.

PLAN STATUS: S0–S8 complete.
