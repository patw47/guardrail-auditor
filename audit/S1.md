# S1 Verification Report — IaC parsers → normalized resource model

Verifier: isolated/read-only. Repo: /home/acer/guardrail-auditor. Date: 2026-06-22.
venv tools used: ./.venv/bin/{ruff,mypy,pytest}.

## Quality ladder (real output)

| Tool | Command | Output | Exit |
|------|---------|--------|------|
| ruff | `ruff check .` | `All checks passed!` | 0 |
| mypy | `mypy .` | `Success: no issues found in 14 source files` | 0 |
| pytest | `pytest -q` | `9 passed, 1 warning in 1.02s` (warning = unrelated starlette/httpx deprecation) | 0 |

## Acceptance criteria

| # | Criterion | Result | Evidence |
|---|-----------|--------|----------|
| 1 | ruff / mypy / pytest all green | PASS | exit 0 / 0 / 0 (see ladder above) |
| 2 | TF multi-resource + nested + comments/blank lines, correct type/name/LINE surviving #, //, /* */ | PASS | tests/fixtures/terraform/commented_multi.tf:6 (`public_bucket`), :14 (`open_ssh`); asserted exactly in tests/test_parsing.py:27 (`== 6`) and :29 (`== 14`). Verified live: `[('public_bucket', 6), ('open_ssh', 14)]`. Line logic: core/parsing/terraform.py:24-46 strips comments preserving line count, then header-scans. |
| 3 | CFN YAML correct logical-id/type/LINE surviving leading comment | PASS | tests/fixtures/cloudformation/template.yaml: `MyBucket` line 7 (comment at line 6), `MySG` line 13; asserted exactly tests/test_parsing.py:49 (`== 7`), :50 (`== 13`). Live: `[('MyBucket', 7), ('MySG', 13)]`. Line from composed node tree core/parsing/cloudformation.py:40-51. |
| 4 | CFN JSON parses to SAME resources as YAML (type + attributes), line refs > 0 | PASS | tests/test_parsing.py:55-62 compares `(type, attributes)` per logical id and asserts `jr.line > 0`. template.json mirrors template.yaml; both go through one loader path (cloudformation.py:54-93). |
| 5 | Malformed TF AND malformed CFN each raise ParseError cleanly | PASS | tests/test_parsing.py:65-72. Live: `malformed.tf: invalid Terraform HCL (UnexpectedToken)` and `malformed.yaml: invalid CloudFormation YAML/JSON (ParserError)` — both ParseError, no stacktrace leak. Re-raise sites terraform.py:70-73, cloudformation.py:59-62. |
| 6 | Empty/comments-only → zero resources, no error | PASS | tests/test_parsing.py:75-77. Live: `empty.tf -> []`, `comments_only.yaml -> []`. Returned via the real parse path (cloudformation.py:64-71 `data is None`/no `Resources`; TF: no `resource` blocks), NOT via a swallowed exception. |
| 7 | Determinism: same fixture twice → identical Resource list | PASS | tests/test_parsing.py:80-83 (`first == second`). Resource is a plain `@dataclass` (models/resource.py:11) so equality is structural; parse path is order-preserving. |

## Refinements

| Ref | Requirement | Result | Evidence |
|-----|-------------|--------|----------|
| R1 | Nested block ALWAYS a list (len 1 for single); test proves single-vs-multiple same list shape | PASS | tests/test_parsing.py:34-41 `test_terraform_nested_block_always_list`: asserts `isinstance(single, list) and len(single) == 1` AND `isinstance(multi, list) and len(multi) == 2`, plus all elements are dicts. This asserts true LIST SHAPE for both, not truthiness. Fixture tests/fixtures/terraform/ingress_shapes.tf: `single` has 1 ingress (lines 2-4), `multi` has 2 (lines 8-13). hcl2 v8 yields blocks as lists; not weakened. |
| R2 | Line-accuracy-under-comments: TF line fixture + one CFN fixture contain #, //, /* */ (TF) / # (CFN) and blank lines BEFORE a resource; test asserts the resource's REAL line | PASS | TF tests/fixtures/terraform/commented_multi.tf: lines 1 (`#`), 2 (`//`), 3-4 (`/* */`), blank line 5, then `resource ... public_bucket` at line 6 → asserted `== 6` (test:27). CFN tests/fixtures/cloudformation/template.yaml: comment at line 6 (`# comment before the bucket`) + blank line 5, then `MyBucket:` at line 7 → asserted `== 7` (test:49). Both assert exact real line, not shifted. |

## Constraints

| Constraint | Result | Evidence |
|------------|--------|----------|
| PARSE ONLY — no detectors/scoring/mapping/ConfigSource/persistence | PASS | Grep of core/ rules/ api/ models/ for detect/scor/severit/map/ConfigSource/persist/finding returns only docstrings/comments (future-sprint notes) and the YAML API call `loader.construct_mapping`/`isinstance(...MappingNode)`. No detection/scoring/mapping logic. rules/__init__.py and api/__init__.py are still one-line stubs. |
| core/db.py still THIN — no domain tables | PASS | Grep for `Column(`/`Mapped[`/`__tablename__`/`class Scan|Finding|Score|Control` → no code hits (only the db.py:4 docstring naming them as future S5 work). core/db.py unchanged vs origin/main (empty diff). |
| New deps limited to python-hcl2 + pyyaml | PASS | pyproject.toml [project.dependencies] diff vs origin adds exactly `python-hcl2>=4.3` and `pyyaml>=6.0` (plus `core.parsing` package registration under [tool.setuptools]). No other new dep. (Note: installed hcl2 is 8.1.2; code targets v8 `__is_block__` semantics — satisfies the `>=4.3` floor; minor: the version pin/comment under-states the real floor but is not a dep violation.) |
| main.py /health untouched | PASS | main.py diff vs origin/main is EMPTY. `/health` returns `{"status": "ok"}` (main.py:26-29). |
| Resource is the single normalized type in models/ | PASS | models/resource.py defines one `Resource` dataclass; models/__init__.py exports only `Format`, `Resource`. No Finding/Score/Control types present. |

## Tampering hunt

Grep over source (excluding .venv/.git/caches) for skip / xfail / pytest.mark.skip / `# type: ignore` / blanket noqa / weakened assertions / swallowing try-except / disabled ruff-mypy rules:

- skip / xfail / pytest.mark.skip / `# type: ignore`: NONE found in source.
- Weakened assertions: NONE. Criteria 2/3 exact lines are asserted exactly — `== 6`, `== 14` (test:27,29), `== 7`, `== 13` (test:49,50). The only `line > 0` assertion is test:62, which is legitimately the JSON cross-check where criterion 4 explicitly permits "line refs > 0" (exact lines belong to the YAML, asserted separately).
- Swallowing try/except to fake zero resources: NONE. terraform.py:70 and cloudformation.py:59 catch parse errors and RE-RAISE as ParseError; the empty-file `[]` results come from the real parse path, confirmed live. parsing/__init__.py:38 catches only UnicodeDecodeError → ParseError.
- Disabled ruff/mypy rules: NONE. No per-file-ignores / ignore_errors / rule disables in pyproject.toml. mypy keeps disallow_untyped_defs + warn_unused_ignores etc.

S506 judgment (core/parsing/cloudformation.py:57, `# noqa: S506 - subclassed SafeLoader`):
LEGITIMATE. The `yaml.load` call uses a SafeLoader subclass (`_CfnLoader(yaml.SafeLoader)`, cloudformation.py:22) — actually safe, not arbitrary-object loading. Furthermore, ruff's lint selection is `["E","F","I","UP","B"]` (pyproject.toml [tool.ruff.lint]); the bandit `S` ruleset is NOT enabled, so this noqa suppresses nothing real — it is purely documentary, marking the SafeLoader-subclass safety. It hides no active error. Not tampering.

Result: NONE FOUND.

## VERDICT

PASS — all 7 acceptance criteria pass, both refinements (R1, R2) pass, all constraints honored, no tampering found (the single S506 noqa is legitimate/documentary).
