# Audit S2 — Detector engine + 4 detectors (first L3 sprint)

Verifier run: 2026-06-22. Repo: /home/acer/guardrail-auditor.
Mode: READ-ONLY (Read/Grep/Bash). Only write: this report. Tooling: `./.venv/bin/{ruff,mypy,pytest}`.

---

## INITIAL VERDICT (first pass): FAIL — H2 documentation leg missing

All six acceptance criteria PASS, H1 PASSES, every detector proven a real discriminator (TP fires +
near-miss silent-by-logic, near-miss carries same resource type as its TP), constraints all hold, no
tampering. The single blocker was **H2**: the severity values in code were correct and AVD/CIS-aligned
(RULES dict, detectors.py:21-24), BUT the REQUIRED AVD/CIS citation in memory/decisions.md was ABSENT
(decisions.md had only S0/S1 sections), and detectors.py:6,19 falsely claimed the rationale was
"documented in decisions.md". Code leg correct, doc leg missing → FAIL.
Prescribed remediation: doc-only — add an S2 section to memory/decisions.md citing the AVD/CIS source
PER severity. No code change needed.

## REMEDIATION APPLIED (builder): doc-only

`git diff --stat HEAD` confirms the remediation touched only memory/decisions.md (+21 lines, a new
`## S2` section). The other tracked diffs are NOT detector logic and pre-existed this remediation as
the sprint's wiring:
- memory/decisions.md  +21  ← the H2 fix (new ## S2 section with AVD/CIS table)
- models/__init__.py   +2/-1 ← re-export Finding/Severity (sprint wiring)
- rules/__init__.py    +6/-1 ← re-export DETECTORS/Detector/run_detectors (sprint wiring)
- prompts.md           +29  ← SCRIBE verbatim log of architect gate prompt #16

Untracked (pre-existing S2 sprint code, NOT created by the remediation):
rules/detectors.py, rules/engine.py, models/finding.py, tests/test_detectors.py,
tests/fixtures/detectors/*. No detector logic / test / fixture was altered to make H2 pass.

---

## RE-CHECK 1 — Tooling ladder (real output + exit codes)

| Tool | Version | Result | Exit |
|------|---------|--------|------|
| ruff check . | 0.15.18 | `All checks passed!` | 0 |
| mypy . | 2.1.0 | `Success: no issues found in 18 source files` | 0 |
| pytest -q | (env) | `23 passed, 1 warning in 0.94s` | 0 |

The single warning is a Starlette/httpx deprecation from FastAPI's TestClient (test_health) — not S2 code.
Ladder still all green → the doc-only remediation broke nothing.

## RE-CHECK 2 — H2 now satisfied (doc present + matches code) → PASS

memory/decisions.md now has a `## S2` section (lines 46-65) with a severity table citing the AVD/CIS
source PER severity:

| rule_id | doc severity (AVD anchor / CIS) | code severity (detectors.py RULES) | match |
|---------|---------------------------------|------------------------------------|-------|
| OPEN_SSH | critical (AVD-AWS-0107 aws-ec2-no-public-ingress-sgr / CIS 5.2) | critical (line 22) | YES |
| PUBLIC_DB | critical (AVD-AWS-0011 aws-rds-no-public-db-access) | critical (line 24) | YES |
| S3_PUBLIC_BUCKET | high (AVD-AWS-0086/0091 / CIS 2.1.5) | high (line 21) | YES |
| UNENCRYPTED_STORAGE | high (AVD-AWS-0027 EBS / AVD-AWS-0080 RDS / CIS 2.2.1) | high (line 23) | YES |

Every cited severity MATCHES the code. No code-vs-doc mismatch. The doc also captures the architect
reconciliation (AVD anchors SSH-open as CRITICAL, so OPEN_SSH > S3_PUBLIC_BUCKET=high) and the H1
Condition-exception rationale. The previously-dangling cross-reference in detectors.py:6,19
("documented in decisions.md") now points at a section that exists.

## RE-CHECK 3 — remediation was DOC-ONLY; severities unchanged in code → PASS

`grep -nE '"(S3_PUBLIC_BUCKET|OPEN_SSH|UNENCRYPTED_STORAGE|PUBLIC_DB)":' rules/detectors.py`:
  21: "S3_PUBLIC_BUCKET": (..., "high")
  22: "OPEN_SSH":         (..., "critical")
  23: "UNENCRYPTED_STORAGE": (..., "high")
  24: "PUBLIC_DB":        (..., "critical")

These are byte-identical to the values recorded in the first audit — the severities were ALREADY
correct in code before the fix; the remediation only added documentation. detectors.py is untracked
and was not modified by the remediation; no test or fixture diff. H2 was made to pass by adding the
missing doc, not by editing code/tests/fixtures down to the doc.

## RE-CHECK 4 — live anti-fake spot-check (s3 policy pair) → HOLDS

Probe parsed both fixtures and ran them through the SAME `detect_public_s3`:
  s3_public_policy.tf:      types=['aws_s3_bucket'] -> ('S3_PUBLIC_BUCKET', 'high', 's3_public_policy.tf', 1)
  s3_policy_conditioned.tf: types=['aws_s3_bucket'] -> SILENT

- TP fires: wildcard Principal "*" Allow, NO Condition → S3_PUBLIC_BUCKET / high / line 1.
- Near-miss silent: same aws_s3_bucket, wildcard Principal "*" Allow BUT scoping Condition
  (aws:SourceIp 10.0.0.0/8) → silent via _statement_is_public (detectors.py:101-103), the exact
  Checkov false positive we deliberately do not flag.
- Both are real aws_s3_bucket resources → silence is a discrimination decision, not parse failure.
  Not always-None (TP fires), not always-fire (NM silent). Anti-fake pair holds live.

---

## FINAL VERDICT: PASS

Ladder green (ruff/mypy/pytest exits 0/0/0); H2 now satisfied (memory/decisions.md ## S2 cites the
AVD/CIS source per severity and every cited severity matches the code RULES dict); the remediation was
DOC-ONLY (only memory/decisions.md content changed for the fix; detector logic, tests, and fixtures
untouched; severities unchanged from the first audit); and the live anti-fake spot-check holds
(s3_public_policy.tf fires S3_PUBLIC_BUCKET high:1, s3_policy_conditioned.tf stays silent through the
same detector). The first-pass H2 blocker is resolved without any code/test/fixture tampering. PASS.

---

## Severity reconciliation re-check (verifier, 2026-06-22)

Focused re-check of the S2 SEVERITY CHANGE only (PUBLIC_DB critical→high; OPEN_SSH stays critical).
Final intended severities: OPEN_SSH=critical, S3_PUBLIC_BUCKET=high, UNENCRYPTED_STORAGE=high, PUBLIC_DB=high.

| source-of-truth | value | agrees? |
|---|---|---|
| `rules/detectors.py` RULES — OPEN_SSH (line 22) | critical | YES |
| `rules/detectors.py` RULES — S3_PUBLIC_BUCKET (line 21) | high | YES |
| `rules/detectors.py` RULES — UNENCRYPTED_STORAGE (line 23) | high | YES |
| `rules/detectors.py` RULES — PUBLIC_DB (line 24) | high | YES |
| `tests/test_detectors.py` test_open_ssh_fires (line 60) | asserts "critical" | YES |
| `tests/test_detectors.py` test_s3_public_acl_fires (line 34) | asserts "high" | YES |
| `tests/test_detectors.py` test_unencrypted_storage_fires (line 77) | asserts "high" | YES |
| `tests/test_detectors.py` test_public_db_fires (line 90) | asserts "high" (NOT critical) | YES |
| `tests/test_detectors.py` — any test asserting PUBLIC_DB critical | none (line 89 is a "not critical" comment; line 108 is the {crit,high,med,low} membership set; line 60 is OPEN_SSH) | YES (none) |
| `memory/decisions.md` S2 table | OPEN_SSH critical; PUBLIC_DB/S3/UNENCRYPTED high — matches code | YES |
| `LIMITATIONS.md` exists | yes | YES |
| `LIMITATIONS.md` — OPEN_SSH AVD(Critical)-vs-rego(HIGH) drift + adjudication kept CRITICAL | recorded | YES |
| `LIMITATIONS.md` — PUBLIC_DB critical→high correction | recorded ("lowered critical→high … current rego says HIGH") | YES |

### Ladder (real output + exit codes)
- `./.venv/bin/ruff check .` → `All checks passed!` — exit 0
- `./.venv/bin/mypy .` → `Success: no issues found in 18 source files` — exit 0
- `./.venv/bin/pytest -q` → `23 passed, 1 warning in 1.38s` — exit 0
  (the 1 warning is the pre-existing Starlette/httpx TestClient deprecation in test_health, not S2 code)
- pytest count still **23 passed** (14 functions in test_detectors.py + the rest of the suite); no test
  silently dropped to dodge the change.

### Tampering check (severity-only, no logic faked)
- `git status --short`: detectors.py / test_detectors.py / fixtures are UNTRACKED (`??`); tracked diffs
  are memory/decisions.md (+33, the S2 doc table), memory/lessons.md, highlights.md, prompts.md (SCRIBE
  logs), and models/__init__.py / rules/__init__.py (sprint re-export wiring). No detector-logic file is
  in the tracked diff.
- `grep -rnE "skip|xfail|@pytest.mark" tests/` → none. No test skipped/xfail'd, no assertion deleted.
- Detector LOGIC unchanged — the severity is sourced ONLY from the RULES dict via `_finding()`
  (detectors.py:43-54); detection predicates (`_statement_is_public`, `_ssh_open`, `_is_false`,
  `_is_true`) untouched. The change is severity-data-only (RULES value + matching test assertion + docs),
  not a logic edit to fake a pass.

### VERDICT: PASS
RULES, tests, decisions.md, and LIMITATIONS.md all agree at the final values (OPEN_SSH=critical;
S3_PUBLIC_BUCKET / UNENCRYPTED_STORAGE / PUBLIC_DB = high); ladder green (ruff/mypy/pytest exit 0/0/0,
23 passed); no logic tampering, no skip/xfail, no deleted assertion, and no test still asserts PUBLIC_DB
critical. The PUBLIC_DB critical→high correction and the OPEN_SSH drift adjudication are both recorded in
LIMITATIONS.md and mirrored in decisions.md.
