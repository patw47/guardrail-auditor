# S4 Verification — Compliance Mapping + Deterministic Explanation

**Sprint:** S4 (compliance mapping + deterministic explanation)
**Verifier:** isolated, read-only
**Date:** 2026-06-22
**Scope:** `core/compliance.py`, `models/control.py`, `tests/test_compliance.py`,
LIMITATIONS.md, memory/decisions.md (vs HEAD `6622a9b`).

## Verification ladder (real output)

| Tool | Command | Result | Exit |
|------|---------|--------|------|
| ruff | `./.venv/bin/ruff check .` | `All checks passed!` | 0 |
| mypy | `./.venv/bin/mypy .` | `Success: no issues found in 24 source files` | 0 |
| pytest | `./.venv/bin/pytest -q` | `39 passed, 1 warning in 0.86s` | 0 |

(The single warning is a pre-existing Starlette/httpx testclient deprecation, unrelated to S4.)
Compliance suite: `tests/test_compliance.py` — **8 passed**, no skip/xfail.

## Acceptance criteria

| # | Criterion | Verdict | Evidence |
|---|-----------|---------|----------|
| 1 | ruff / mypy / pytest all green | **PASS** | exit 0 for all three (see ladder above) |
| 2 | Every seed rule_id → ≥1 NAMED control {CIS,SOC2,ISO27001,GDPR}, each with https ref | **PASS** | `core/compliance.py:41-62` CONTROL_MAP; all 4 rule_ids mapped; test `test_every_rule_maps_at_least_one_named_control` (asserts framework ∈ named set AND `reference_url.startswith("https://")`). All URLs https (CIS/SOC2/ISO/GDPR32 constants `core/compliance.py:32-38`). |
| 3 | Governing rule: AVD_ANCHOR[rule_id] severity == RULES[rule_id] severity for all four | **PASS** | live: OPEN_SSH critical==critical, S3 high==high, UNENCRYPTED high==high, PUBLIC_DB high==high. Test `test_governing_rule_avd_severity_equals_detector_severity` (`tests/test_compliance.py:35-38`). AVD_ANCHOR `core/compliance.py:17-30`, RULES `rules/detectors.py:20-25`. |
| 4 | ADAPTER §1 fence: map_finding returns only CONTROL_MAP controls; unknown rule_id → []; no mutation of table/finding | **PASS** | `core/compliance.py:65-68` returns `list(CONTROL_MAP.get(rule_id, []))` (copy). Live: `map_finding(UNKNOWN)==[]`; table len stable 3→3 after caller appends. Tests `test_fence_no_control_id_outside_the_table`, `test_map_finding_does_not_mutate_the_table`. |
| 5 | render() pure & deterministic; no LLM/network; stdlib+models imports only | **PASS** | `core/compliance.py` imports only `models` (`:11-12`); `models/control.py` imports only `dataclasses`,`typing` (`:10-11`). Forbidden-import grep (httpx/requests/openai/anthropic/yaml/hcl2/socket/urllib/aiohttp/http.client) → NONE. Live: render byte-identical on repeat. Test `test_render_is_deterministic`. |
| 6 | No-line case: line=0 → no ":None", no ":0"; location degrades to file only | **PASS** | `_location` `core/compliance.py:71-72` returns bare `file` when `line` falsy. Live line=0 → `... at main.tf: ev ...`, no `:None`, no `main.tf:0`. Test `test_render_no_line_has_no_none`. |

## Architect-required hardenings

| H | Requirement | Verdict | Evidence |
|---|-------------|---------|----------|
| H1 | AVD dropped from Framework enum; render/label never emit "AVD" as a control | **PASS** | `models/control.py:13` `Framework = Literal["CIS", "SOC2", "ISO27001", "GDPR"]` — no AVD. No "AVD" string anywhere in control.py. In compliance.py "AVD" appears ONLY in the separate `AVD_ANCHOR` table (`:14-30`), never in CONTROL_MAP or render. |
| H2 | Section cite (PUBLIC_DB CIS §2.3) renders visible "(section)"; precise cite (OPEN_SSH CIS §5.2) does NOT | **PASS** | `Control.label` `models/control.py:33-37` appends `" (section)"` iff `level=="section"`. Live render: PUBLIC_DB → `§2.3 (section)` present; OPEN_SSH → no `(section)`. Test `test_section_marker_is_visible_only_for_section_cites`. |
| H3 | Verified ids: SSH CIS 5.2; S3 CIS 2.1.4 (NOT 2.1.5); UNENC CIS 2.2.1; SOC2 CC6.1 on SSH+S3; ISO A.8.20/A.5.15/A.8.24; GDPR Art.32 on S3+UNENC; PUBLIC_DB primary ISO A.8.20, CIS §2.3 section | **PASS** | Machine cross-check of CONTROL_MAP vs required set: all 4 rule_ids OK exact-match. PUBLIC_DB primary (first entry) == ISO A.8.20. No `2.1.5` anywhere. `core/compliance.py:42-62`. |
| H4 | LIMITATIONS.md records CIS RDS sub-number-unverified → section-level decision | **PASS** | `LIMITATIONS.md:26-34` "PUBLIC_DB → CIS: precise sub-number unverified, cited at section level" — documents §2.3 (section), primary ISO A.8.20, and confirms other CIS ids §5.2/§2.1.4/§2.2.1. |

## Live render — all four findings (core.parsing + rules + core.compliance over tests/fixtures/detectors/multi_violation.tf)

- **OPEN_SSH** (critical, line 6): `CRITICAL — Security group allows SSH from 0.0.0.0/0. aws_security_group 'sg' at multi_violation.tf:6: ingress permits TCP 22 from 0.0.0.0/0. Compliance: CIS AWS v3.0.0 §5.2; SOC 2 CC6.1; ISO 27001:2022 A.8.20. Reference: https://www.cisecurity.org/benchmark/amazon_web_services.`
- **S3_PUBLIC_BUCKET** (high, line 1): `HIGH — Publicly accessible S3 bucket. aws_s3_bucket 'b' at multi_violation.tf:1: ACL 'public-read' grants public access. Compliance: CIS AWS v3.0.0 §2.1.4; SOC 2 CC6.1; ISO 27001:2022 A.5.15; GDPR Art. 32. Reference: https://www.cisecurity.org/benchmark/amazon_web_services.`
- **UNENCRYPTED_STORAGE** (high, line 14): `HIGH — Storage resource is not encrypted at rest. aws_ebs_volume 'v' at multi_violation.tf:14: encrypted is false — storage unencrypted at rest. Compliance: CIS AWS v3.0.0 §2.2.1; ISO 27001:2022 A.8.24; GDPR Art. 32. Reference: https://www.cisecurity.org/benchmark/amazon_web_services.`
- **PUBLIC_DB** (high, line 19): `HIGH — Database instance is publicly accessible. aws_db_instance 'd' at multi_violation.tf:19: publicly_accessible is true — database reachable from the internet. Compliance: ISO 27001:2022 A.8.20; CIS AWS v3.0.0 §2.3 (section). Reference: https://www.iso.org/standard/27001.`

PUBLIC_DB Reference points at the ISO primary (A.8.20), not the section-level CIS cite — correct primary-first ordering.

## Tampering / credibility hunt

- **No fabricated precise control id.** The ONLY section-level cite is `PUBLIC_DB` CIS §2.3, explicitly `level="section"` (`core/compliance.py:60`). Machine scan confirms `section-level cites == [('PUBLIC_DB','CIS','2.3')]`. Every other cite is a verified precise id (SSH §5.2, S3 §2.1.4, EBS §2.2.1, SOC2 CC6.1, ISO A.8.20/A.5.15/A.8.24, GDPR Art.32). No precise id appears to be a guess.
- **No 2.1.5 drift.** `grep 2.1.5` → none; S3 BPA is the v3.0.0 §2.1.4.
- **No skip/xfail/weakened assertions.** `grep -rEi "skip|xfail|@pytest.mark|assert True"` over `tests/*.py` source → NONE. The fence test asserts membership + unknown→[] ; the section-marker test asserts both presence (PUBLIC_DB) and absence (OPEN_SSH); the governing-rule test asserts equality for every RULES entry and that an anchor exists. All assert real conditions.
- **AVD kept out of the compliance surface.** Framework Literal has no AVD; AVD lives only in the severity-provenance table consumed by the governing-rule test.

## Constraints (no out-of-scope ship)

- `git diff --stat HEAD` for `rules/ core/scoring.py core/parsing/ api/ core/db.py` → **empty** (unchanged vs HEAD).
- `git diff --stat HEAD pyproject.toml` → **empty** — no new dependency.
- `api/__init__.py` still a one-line stub ("Fills at S5"). `core/db.py` tableless — no domain tables register on `Base` (no-op `init_db`).
- New/changed files this sprint: `core/compliance.py`, `models/control.py`, `tests/test_compliance.py` (new); `models/__init__.py` (exports Control/Framework), LIMITATIONS.md, memory/decisions.md, prompts.md (docs). All in-scope.

## VERDICT

**PASS** — All six acceptance criteria pass; hardenings H1–H4 pass; render() is pure and deterministic (stdlib + models only, no LLM/network); no fabricated precise control id (sole section-level cite is PUBLIC_DB CIS §2.3, explicitly marked); no tampering, skips, or weakened assertions; out-of-scope paths and dependencies unchanged vs HEAD.
