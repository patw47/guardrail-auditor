# S7 Verification — adversarial assurance (coverage matrix + Checkov/TerraGoat oracle)

Verifier: isolated, read-only. Date 2026-06-22. Repo `/home/acer/guardrail-auditor`, branch `main`.

## L1 gates (real output)
| gate | command | result | exit |
|------|---------|--------|------|
| ruff | `./.venv/bin/ruff check .` | `All checks passed!` | 0 |
| mypy | `./.venv/bin/mypy .` | `Success: no issues found in 37 source files` | 0 |
| pytest | `./.venv/bin/pytest -q` | `60 passed, 1 warning` | 0 |

The 1 warning is an upstream Starlette/httpx deprecation, not a test issue.

## Per-criterion verdicts
| # | Criterion | Verdict | Evidence |
|---|-----------|---------|----------|
| 1 | L1 ruff/mypy/pytest green | PASS | above; exit 0 all three |
| 2 | matrix built from real `pytest --junitxml`, reproducible | PASS | `tools/coverage_matrix.py:28-44` runs `pytest … --junitxml` and parses JUnit XML; verdict = `not failed` per testcase. Re-ran generator: md5 `ddf8e3d2…` before == after (unchanged). File is untracked (new), so reproducibility = stable content hash across regen. |
| 3 | per-detector TP fires + near-miss silent incl. hard conditioned near-miss | PASS | `docs/coverage_matrix.md:11-22` — all 4 detectors have TP(fires)+near-miss(silent); row 17 = `s3_policy_conditioned.tf → test_s3_conditioned_policy_silent → PASS (silent)` |
| 4 | guard test asserts proving_test exists / TP+near-miss per RULE / hard labelled / no unlabelled discrimination test / committed matrix matches manifest | PASS | `tests/oracle/test_coverage_matrix.py:41-81` — 5 real asserts (not stubs), all pass |
| 5 | pins committed: Checkov 3.2.334 + TerraGoat SHA + reproduce cmd + reference.json | PASS | `tests/oracle/SOURCES.md:10-20`; `checkov_reference.json` present (15 entries) |
| 6 | reference is GENUINE Checkov output, not synthesized | PASS | see Part B genuineness judgment below |
| 7 | K→K agreement honest about completeness, runs offline | PASS | `test_oracle_agreement.py:41-56`; 4 tests pass offline, no checkov import / no network |
| 8 | divergence types labelled: TYPE(a) WIN vs TYPE(b) limitation | PASS | `LIMITATIONS.md:50-59` separates TYPE(b) CKV2_AWS_6 (out of subset) from TYPE(a) WIN attributed to Part-A corpus |
| 9 | two streams kept DISTINCT | PASS | matrix header + SOURCES.md + oracle test docstrings all call the streams distinct; never conflated |

## PART A — coverage matrix (own-corpus discrimination)
- Generator `tools/coverage_matrix.py` joins `tests/oracle/manifest.yaml` (12 labelled cases) to **real** pytest outcomes: `_pytest_outcomes()` shells `python -m pytest tests/test_detectors.py -q --junitxml <tmp>`, parses `<testcase>` nodes, marks a test failed if it has `failure/error/skipped`. Verdict column is the actual result, not hand-run.
- Re-ran `./.venv/bin/python tools/coverage_matrix.py` (exit 0). `docs/coverage_matrix.md` md5 `ddf8e3d2b94f6932668cf2df9f794c43` identical before and after → reproducible. (File is untracked/new this sprint, so `git diff` is empty by construction; content hash is the reproducibility evidence.)
- Matrix shows 12 rows = 7 `PASS (fires)` + 5 `PASS (silent)`; every detector in `RULES` has a TP and a near-miss. The HARD conditioned-policy near-miss is present and labelled (`docs/coverage_matrix.md:17`): wildcard Principal `*` + scoping `aws:SourceIp` Condition → silent, proven by `test_s3_conditioned_policy_silent`. Detector logic `rules/detectors.py:96-103` correctly treats a `Condition` as non-public (the exact Checkov-style false positive avoided).
- Guard `tests/oracle/test_coverage_matrix.py` asserts the four required properties with real conditions (proving-test existence by source grep; TP+near-miss per `RULES`; hard near-miss labelled; no `_fires`/`_silent` discrimination test orphaned from the manifest; committed-matrix row count == manifest case count and every test name present). Not stubs — all 5 pass and would fail on drift.

## PART B — Checkov × TerraGoat oracle
- `SOURCES.md` pins Checkov `3.2.334`, TerraGoat `729f8da62c6a85ce4af5ad3d123de97776d954c4`, and the exact reproduce command (clone --depth 1, checkout SHA, install pinned checkov, `checkov -d … --compact -o json`).
- `test_oracle_agreement.py` runs MY scanner (`core.pipeline.run_scan` over the vendored `tests/oracle/terragoat/*.tf`) and compares to the committed reference on the implemented subset `{OPEN_SSH→CKV_AWS_24, PUBLIC_DB→CKV_AWS_17, UNENCRYPTED_STORAGE→CKV_AWS_16}`. All three: mine flags AND Checkov FAILED → agree 3/3.
- S3 honesty: `S3_PUBLIC_BUCKET` has NO positive case — `test_s3_has_no_positive_oracle_case` asserts both my tool is silent AND `CKV_AWS_20 == set()` (no FAILED). Verified: NO `public-read*` ACL exists anywhere in vendored TerraGoat (all ACLs `private`/`log-delivery-write`), so CKV_AWS_20 PASSED is a real Checkov verdict. Stated openly in SOURCES.md and LIMITATIONS.md, not hidden.
- OFFLINE: oracle tests import only `core.*` + stdlib `json`, read the committed JSON from disk, run the scanner locally. No `import checkov`, no network. Confirmed by grep and by `checkov` being absent from the venv (`ModuleNotFoundError`).

### Genuineness judgment on checkov_reference.json: GENUINE (not hand-authored)
Multiple independent corroborations:
1. Real check IDs with Checkov's exact wording: CKV_AWS_24 ("Ensure no security groups allow ingress from 0.0.0.0:0 to port 22"), CKV_AWS_16 (RDS encrypted at rest), CKV_AWS_17 (RDS not publicly accessible), CKV_AWS_20 (S3 public-read ACL), CKV2_AWS_6 (S3 Public Access block).
2. Real `check_result.result` PASSED/FAILED shape, real `file_path` (`/s3.tf` etc.) and `file_line_range`.
3. Line ranges match the vendored files exactly: web-node SG @ ec2.tf L77 (ref 77-115); RDS default @ db-app.tf L1 (ref 1-42); flowbucket @ ec2.tf L271 (ref 271-288); data bucket @ s3.tf L1 (ref 1-21).
4. Verdicts are consistent with file reality and would be wrong if fabricated: RDS `storage_encrypted=false` + `publicly_accessible=true` → CKV_AWS_16/17 FAILED; no public ACL anywhere → CKV_AWS_20 PASSED on all 6 buckets; all buckets lack a public-access block → CKV2_AWS_6 FAILED on all 6. A convenience file would not include CKV2_AWS_6 FAILED entries that the tests do not even consume.
No signs of hand-authoring.

### Divergences labelled (refinement 1)
`LIMITATIONS.md:50-59`: TYPE (b) honest limitation = CKV2_AWS_6 (block-public-access) flagged by Checkov on 6 buckets but outside my implemented subset (correctly NOT in `RULE_TO_CKV`, so never claimed as a miss/agreement). TYPE (a) WIN = none on TerraGoat; the stricter-and-correct WIN (silent on the Checkov-style false positive) is demonstrated on the Part-A corpus `s3_policy_conditioned.tf`, kept DISTINCT from the oracle.

### Streams distinct (refinement 3)
Part A (own-corpus discrimination) and Part B (agreement-with-reference on TerraGoat) are labelled distinct in the matrix header, SOURCES.md, LIMITATIONS.md, and both oracle test docstrings. No claim conflates them.

## Constraints
| ID | Constraint | Verdict | Evidence |
|----|-----------|---------|----------|
| C1 | no new detectors; no new RUNTIME dep | PASS | `git diff --stat HEAD rules/detectors.py` empty; `pyproject` diff adds checkov ONLY to `[optional-dependencies] oracle`, `[project.dependencies]` unchanged; checkov not installed (`ModuleNotFoundError`) |
| C2 | pipeline logic unchanged | PASS | `git diff --stat HEAD core/ models/` empty; no untracked files under core/models/rules |
| C3 | vendored secrets redacted, detection unchanged | PASS | grep `AKIA[0-9A-Z]{16}` = none; `wJalrXUtnFEMI` = none; no PRIVATE KEY blocks; AWS creds are `REDACTED-FOR-COMMIT`; agreement test still finds OPEN_SSH/PUBLIC_DB/UNENCRYPTED_STORAGE (redaction touched only credential strings, not detector-read attrs) |

Changed vs HEAD (`git diff --stat HEAD`): only `LIMITATIONS.md`, `prompts.md`, `pyproject.toml` (+66 lines). New untracked: `docs/coverage_matrix.md`, `tests/oracle/`, `tools/`. No source/core/model/detector code touched.

## Tampering hunt
No skip/xfail/xpass/pytest.mark, no weakened asserts, no `assert True`/`...`/stub returns in `tests/oracle/` or `tests/test_detectors.py` (sole "skip" hit = `skip_final_snapshot` TF attribute in a fixture). Guard + oracle tests assert real conditions and all 9 pass. The Checkov reference is genuine (see above), not a fabricated convenience file.

## VERDICT: PASS
All 9 criteria pass. The matrix is provably CI-sourced (real pytest --junitxml, reproducible hash). The Checkov reference is genuine (real IDs/shape/line-ranges, verdicts consistent with file reality). K→K is honest about completeness (S3 no-positive-case stated, not hidden). The two evidence streams are kept distinct. No new runtime dependency (checkov optional/CI-only, not installed) and no new detector. Constraints C1–C3 satisfied; no tampering.
