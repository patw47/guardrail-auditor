# S6 — Dashboard (MVP-closing sprint) — VERIFIER report

Scope verified READ-ONLY against working tree (S6 changes uncommitted).
venv: `./.venv/bin/{ruff,mypy,pytest}`. Live probe on an isolated `/tmp` DB.

## Verification ladder (real output + exit codes)
| Gate | Result | Exit |
|------|--------|------|
| `ruff check .` | `All checks passed!` | 0 |
| `mypy .` | `Success: no issues found in 33 source files` | 0 |
| `pytest -q` | `51 passed, 1 warning in 0.87s` | 0 |

(The single warning is a Starlette/httpx deprecation in FastAPI's TestClient — not our code.)

## Acceptance criteria
| # | Criterion | Verdict | Evidence |
|---|-----------|---------|----------|
| 1 | ruff/mypy/pytest green | PASS | all exit 0, output above |
| 2 | `GET "/"` → 200 HTML dashboard shell | PASS | live: `status=200 content_type=text/html`. Shell tokens present (test + live): `id="files"`/`id="scan"` web/index.html:16-17; "Risk Score" :31; `id="gauge-arc"` :26; headers `>Severity<`/`>Compliance<` :41-42; `id="empty"` :36. Test: tests/test_dashboard.py:24-33 |
| 3 | `/static/app.js` & `/static/styles.css` → 200 | PASS | live both 200 (`text/javascript`, `text/css`). Test: tests/test_dashboard.py:36-39 |
| 4 | Behavioral: POST multi_violation carries every rendered field; score=95/grade=F | PASS | live POST → HTTP 201, `score.value=95 grade=F`, counts{crit1,high3}. finding keys ⊇ {severity,title,resource_type,resource_name,file,line,explanation,controls}; control keys ⊇ {framework,control_id,level,reference_url,label}. Test: tests/test_dashboard.py:59-77 |
| 5 | Empty state: clean fixture → score 0/A, findings []; app.js has empty branch | PASS | live POST clean.tf → 201, value=0 grade=A findings=[]. app.js empty branch web/static/app.js:64-65 (`$("empty").hidden=...`). Test: tests/test_dashboard.py:80-87 |
| 6 | Error state: malformed → 400; app.js has error branch (showError) | PASS | live POST malformed.tf → 400 `{"detail":"...invalid Terraform HCL..."}`. app.js `showError` web/static/app.js:18,29. Test: tests/test_dashboard.py:90-96 |
| 7 | Severity by colour AND text (not colour-only) | PASS | colour: styles.css `.sev-critical/.sev-high/.sev-medium/.sev-low` :41-44. text: app.js `badge.textContent=f.severity.toUpperCase()` :73. Test: tests/test_dashboard.py:51-56 |

## Architect decisions
- **D1 — control label single-source: CONFIRMED.** `ControlOut.label` computed in API from domain `Control.label` (api/routes.py `_control_out` reconstructs a `Control` and returns `domain.label`, lines added in diff). `Control.label` is the only place the `§…(section)` marker is built (models/control.py:32-37). app.js renders `a.textContent = c.label` (web/static/app.js:88) and does NOT re-implement `_FRAMEWORK_LABEL` or the section marker (grep for `CIS AWS|SOC 2|ISO 27001|_FRAMEWORK|section` in app.js → none). Section marker reaches a rendered label LIVE: `CIS AWS v3.0.0 §2.3 (section)`.
- **D2 — Rescan deferred to S5b: CONFIRMED documented, not dropped.** memory/decisions.md S6 block: "DEFERRED — Rescan button → S5b … Recorded decision, not an omission." No Rescan control shipped (grep `rescan` in web/, api/, main.py → none).

## Constraints
- **C1 — dashboard consumes /api ONLY, no business logic in JS: PASS.** Grep app.js for `WEIGHT|threshold|grade bands|score arithmetic|framework-label map` → only hits are the `renderGauge(score)` fn name and reads of `score.value`/`score.grade`/`score.counts` from the API response. No scoring/severity/weight/grade computation.
- **C2 — no JS framework / no build step / NO new runtime dep: PASS.** `git diff HEAD -- pyproject.toml` empty; `[project.dependencies]` unchanged, ends at `python-multipart>=0.0.20` (S5), nothing added at S6. web/ is plain html/css/js (no package.json/bundler/framework import).
- **C3 — core logic unchanged: PASS.** `git diff --stat HEAD` for core/parsing/, rules/, core/scoring.py, core/compliance.py, core/pipeline.py, core/repository.py, core/tables.py, models/* → all empty. Code touched = exactly api/schemas.py (+label field), api/routes.py (_control_out computes label), main.py (serve "/" + mount /static), plus new web/ + tests/fixtures/clean/ + tests/test_dashboard.py. (Other modified files are docs only: SPRINTS.md, memory/decisions.md, prompts.md.) Nothing else flagged.
- **C4 — DB isolated in tests: PASS.** tests/conftest.py sets a temp `DATABASE_URL` before `core.db` import. No `guardrail.db` in repo after full pytest run or live probe (live probe pointed at `/tmp`).

## Tampering hunt
CLEAN. Grep across tests/ for `skip|xfail|pytest.mark|assert True|.keys()==|subset` → none. The contract test (tests/test_dashboard.py:59-77) asserts the REAL field sets via `{...} <= keys()` (requires AT LEAST these fields — correct direction, not a subset trick) and pins value==95/grade=="F". Empty test hits the real API (201, 0/A, findings==[]); error test hits the real API (400). Fixtures are genuine: clean.tf is a real near-miss-safe config; malformed.tf is real broken HCL. No stubs.

## VERDICT: PASS
All 7 criteria, D1+D2, and C1–C4 pass. Dashboard is API-only (no business logic in JS), no new runtime dependency, no tampering. ruff/mypy/pytest all green (51 passed).

## MVP status
S0–S6 complete: parsers → detectors → severity-weighted Risk Score → compliance mapping → REST API + SQLite persistence → served dashboard. MVP CLOSED.
