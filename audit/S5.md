# S5 Verification — REST API + uploaded-files ConfigSource + persistence

Verifier: isolated, read-only. Date: 2026-06-22. Repo: /home/acer/guardrail-auditor

## Tool ladder (real output)

| Tool | Result | Exit |
|------|--------|------|
| ruff check . | `All checks passed!` | 0 |
| mypy . | `Success: no issues found in 32 source files` | 0 |
| pytest -q | `44 passed, 1 warning in 1.32s` | 0 |

(The single warning is a Starlette/httpx TestClient deprecation note from FastAPI, not a test issue.)

## Acceptance criteria

| # | Criterion | Verdict | Evidence |
|---|-----------|---------|----------|
| 1 | ruff / mypy / pytest green | PASS | see ladder above (all exit 0) |
| 2 | POST vulnerable fixture → 201, score+grade+findings, each finding has controls+explanation, row persisted | PASS | test `test_post_scan_returns_201_with_score_and_findings` tests/test_api.py:24-38. Live probe: POST→201, score value=95 grade=F, 4 findings, finding0 has 4 controls + explanation; raw SQL shows scans=1, findings=4, controls=12 persisted. api/routes.py:94-111 |
| 3 | Round-trip persisted-vs-PIPELINE (load_result(session,id) == run_scan(input)) | PASS | tests/test_api.py:41-46 runs `run_scan(...)`, `save_scan`, then `load_result` and asserts `loaded == result`. core/repository.py:75-128 `load_result` reconstructs ScanResult from DB rows (ScanRow/ScoreRow/FindingRow/ControlRow → Finding/Control/MappedFinding/ScoreSummary). Live probe: loaded==result True, `loaded is result` False (distinct object), and corrupting a persisted `scores.value` makes `loaded2 == result` False — genuinely persisted-vs-pipeline, not persisted-vs-itself. |
| 4 | GET endpoints return persisted scan; unknown id → 404 | PASS | tests/test_api.py:36-38 (200s) and test_unknown_scan_returns_404:49-51. Live: GET detail/findings/score all 200, list 200; unknown id 404 on detail, findings, and score. api/routes.py:119-140 |
| 5 | OpenAPI documents every endpoint | PASS | test_openapi_documents_every_endpoint tests/test_api.py:59-66. Live /openapi.json: `/api/scans` methods=[get,post]; `/api/scans/{scan_id}`, `/api/scans/{scan_id}/findings`, `/api/scans/{scan_id}/score` all present (get). |
| 6 | Determinism: same upload twice → identical findings+score | PASS | test_same_upload_is_deterministic tests/test_api.py:54-56 asserts `run_scan(src) == run_scan(src)`. Pipeline is pure/sorted (core/pipeline.py:40-69). |

## Schema vs approved (sensitive gate)

| ID | Check | Verdict | Evidence |
|----|-------|---------|----------|
| S1 | Exactly 4 tables (scans, scores, findings, controls); columns + cascades; control.level persisted | PASS | core/tables.py: `__tablename__` = scans(18), scores(34), findings(50), controls(72) — exactly 4 (also confirmed by live `sqlite_master`: controls, findings, scans, scores). Cascades: Scan 1-1 Score `uselist=False, cascade="all, delete-orphan"` (tables.py:25-27); Scan 1-* Finding `cascade="all, delete-orphan"` (28-30); Finding 1-* Control `cascade="all, delete-orphan"` (66-68); FK ondelete=CASCADE on scores/findings/controls. `ControlRow.level` is a persisted column (tables.py:80); live DB shows distinct levels ['precise','section']. |
| S2 | ORM separate from pure dataclasses; repository maps; models/ has no SQLAlchemy import | PASS | ORM in core/tables.py (SQLAlchemy Mapped/relationship); pure dataclasses in models/ (Finding tests, Control frozen). core/repository.py maps both directions (save_scan 20-64, load_result 75-128). `grep sqlalchemy models/` → no matches. |

## Scope / constraints

| ID | Check | Verdict | Evidence |
|----|-------|---------|----------|
| C1 | Uploaded-files source ONLY; no repo-URL/clone/network | PASS | core/config_source.py contains only `ConfigSource` Protocol (14) + `UploadedFilesSource` (23). Grep of core/ api/ for clone/git/requests/httpx/urllib/socket/subprocess/paramiko/pygit2 → NO MATCHES (TestClient excluded; it lives only in tests/). |
| C2 | Pipeline reused UNCHANGED | PASS | `git diff --stat HEAD` for core/parsing/, rules/, core/scoring.py, core/compliance.py, models/finding.py, models/score.py, models/control.py, models/resource.py → empty (no changes). models/__init__.py diff → empty (no re-export tampering). |
| C3 | DB isolation; tests don't write dev guardrail.db | PASS | tests/conftest.py:9-15 sets `DATABASE_URL` to a temp NamedTemporaryFile .db BEFORE importing core.db (noqa E402 markers). `git status` shows no guardrail.db; .gitignore lines 65/67 ignore `*.db` and `guardrail.db`; no dev guardrail.db on disk. core/db.py:15 reads env-var with dev fallback. |
| C4 | Only new dep = python-multipart | PASS | pyproject.toml diff adds exactly `python-multipart>=0.0.20` to [project.dependencies] (line 17) and the flake8-bugbear extend-immutable-calls config for fastapi.Depends/File (config, not a dep — legitimate FastAPI default-arg pattern). httpx remains in [dev] optional-deps (test client), not runtime. No other runtime dep added. |

Legitimate non-pipeline changes confirmed: core/db.py (env-var DATABASE_URL + `from core import tables` registration in init_db), main.py (include api.routes router under /api). prompts.md changed (governance log, out of scope).

## Tampering hunt

- No skip/xfail/xpass/pytest.mark/type:ignore in tests/ (grep → none).
- Round-trip assertion is a real full-structure equality: `assert loaded == result` over dataclasses with default structural eq (ScanResult/ScoreSummary/MappedFinding/Finding are @dataclass, Control frozen). Not a subset/field-cherry-pick. Negative control (corrupt persisted score → equality False) proves it.
- No try/except swallowing a persistence error: core/repository.py has no try/except. api/routes.py try/except blocks are the db-session generator cleanup (23) and ParseError→400 (103-106); the post-save `get_scan is None` branch raises 500 rather than faking success (109-111).

## VERDICT: PASS

All 6 acceptance criteria pass; schema S1/S2 pass (4 tables, correct cascades, persisted control level, ORM/dataclass separation); C1–C4 pass (uploaded-files only, pipeline unchanged, DB isolated, only python-multipart added). Round-trip is genuinely persisted-vs-pipeline (live-verified: distinct reconstructed object equals run_scan output, breaks on DB corruption). No out-of-scope network/clone code, no tampering.
